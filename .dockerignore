# Ignore node_modules from local dev — we install fresh in Docker
node_modules

# Svelte output folders (if they exist in your local dev env)
dist
build
.svelte-kit

# Git and other VCS
.git
.gitignore

# Editor & IDE configs
.vscode
.idea

# OS-specific
.DS_Store
Thumbs.db

# Environment/config files with secrets (if any)
.env
.env.local

# Log files
*.log

# Docker artifacts
docker-compose.yml
Dockerfile
